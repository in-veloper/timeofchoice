import { View } from "react-native"

interface ListResultProps {
    options: string[]
}

const ListResult: React.FC<ListResultProps> = ({ options }) => {
    return (
        <View>

        </View>
    )
}

export default ListResult