import { View } from "react-native"

interface CoinResultProps {
    options: string[]
}

const CoinResult: React.FC<CoinResultProps> = ({ options }) => {
    return (
        <View>

        </View>
    )
}

export default CoinResult